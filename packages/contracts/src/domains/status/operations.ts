/**
 * ZeroLeak AI has no published releases to check: update checks would either
 * reach the network (contradicting the air-gapped posture) or point at the
 * upstream Nerve releases, so the latest-release operation is removed.
 */
export const statusOperationDefinitions = [] as const;
