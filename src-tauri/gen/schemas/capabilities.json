{"default":{"identifier":"default","description":"The complete set of built-in Tauri commands this frontend uses. Deliberately narrow rather than core:default: the frontend listens for core events and drags the custom title bar, and nothing else. Every other capability of the app is an application-defined command in main.rs, which is where the auditing, workspace boundary and sandbox policy are enforced.","local":true,"windows":["main"],"permissions":["core:event:allow-listen","core:event:allow-unlisten","core:window:allow-start-dragging","core:window:allow-internal-toggle-maximize"]}}